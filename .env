# Public origin where this app is served (scheme + host, no trailing slash).
# Used to build the OAuth client metadata document and redirect URI.
# In dev, the loopback OAuth client is used instead and this is ignored.
VITE_PUBLIC_ORIGIN=https://atproto-qr.app
